/**
 * Формат підпису PAdES
 */
export type PAdESSignLevel = {
  /**
   * Базовий формат підпису. Включає позначку часу
   * від даних та сертифікат підписувача
   */
  PAdES_B_B: number;

  /**
   * Підис PAdES_B_B, який додатково включає позначку
   * часу від ЕЦП
   */
  PAdES_B_T: number;
};
