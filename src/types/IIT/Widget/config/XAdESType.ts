/**
 * Тип підпису XAdES
 */
export type XAdESType = {
  /**
   * Зовнішній (дані та файл зберігаються окремо)
   */
  Detached: number;

  /**
   * Внутрішній (дані та файл зберігаються в одному файлі).
   * Може використовуватися тільки для підпису xml-документів
   */
  Enveloped: number;
};
