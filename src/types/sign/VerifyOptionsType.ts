export type VerifyOptionsType = {
  /** Base64 підписаних даних (для відокремленого підпису) */
  data?: string;
  /** Посилання на підписані дані — завантажує сервер розшифровки */
  dataUrl?: string;
  /** Base64 підпису */
  sign?: string;
  /** Посилання на файл підпису — завантажує сервер розшифровки */
  signUrl?: string;
};
