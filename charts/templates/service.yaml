apiVersion: v1
kind: Service
metadata:
  name: {{ include "prozorro-eds.fullname" . }}
  labels:
    {{- include "prozorro-eds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prozorro-eds.selectorLabels" . | nindent 4 }}
