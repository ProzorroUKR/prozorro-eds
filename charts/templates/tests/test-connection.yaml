apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prozorro-eds.fullname" . }}-test-connection"
  labels:
    {{- include "prozorro-eds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prozorro-eds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
